# Runtime dependencies are installed separately from the repository's uv.lock.
# Refresh this tooling lock with the command recorded in the generated .txt file.
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
setuptools==84.0.0
